import 'package:flutter_test/flutter_test.dart';
import 'package:peloton_communicator/classes/audio_recording.dart';

void main() {
  group('AudioRecorder', () {
    test('Initial state is not recording', () {
      final audioRecorder = AudioRecording();
      // expect(audioRecorder., false);
    });
  });
}
